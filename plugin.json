{
  "name": "pr-quality-gates",
  "version": "0.2.0",
  "description": "8-gate PR validation for Go projects: CCN, coverage, mutation, file size, coupling, SAST, supply chain, secrets.",
  "author": "JohnPitter",
  "license": "MIT",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "command": "hooks/pre-commit.sh"
      }
    ]
  },
  "commands": [
    "commands/quality-report.md"
  ]
}
